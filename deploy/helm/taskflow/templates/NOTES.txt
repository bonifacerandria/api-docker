TaskFlow ({{ .Chart.AppVersion }}) est déployé (release "{{ .Release.Name }}", namespace {{ .Release.Namespace }}).

Vérifier l'état :
  kubectl get pods -n {{ .Release.Namespace }}
  kubectl get jobs -n {{ .Release.Namespace }}   # la migration doit être "Completed"

Tester sans exposition publique (comme au module 16) :
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "taskflow.fullname" . }}-api 8081:80
  curl http://localhost:8081/health

Voir l'historique des déploiements de cette release :
  helm history {{ .Release.Name }} -n {{ .Release.Namespace }}

Revenir à la révision précédente si besoin :
  helm rollback {{ .Release.Name }} -n {{ .Release.Namespace }}
